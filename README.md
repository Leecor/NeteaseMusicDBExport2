# NeteaseMusicDBExport2
网易云音乐2.10本地数据库导出歌单列表

因数据库结构不同，上游项目在网易云2.10运行解析数据库结果空白，故修改数据库解析并增添以下特性：
- 按收藏歌单、创建歌单分类导出
- 每个歌单导出为一个csv文件，并生成一个所有歌单汇总csv文件，方便筛选处理
- 导出的csv文件包含歌曲索引、歌曲名称、歌手名称、专辑名称

# 需要环境
- Python3

# 用法
### Windows
只需要一个test.py文件
```python test.py```
即可

